# Dépendances de l'application web
fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# Dépendances du moteur de CV embarqué (engine/) : reportlab pour le rendu PDF,
# pymupdf pour relire le PDF produit (nombre de pages).
reportlab>=4.0
pymupdf>=1.24
